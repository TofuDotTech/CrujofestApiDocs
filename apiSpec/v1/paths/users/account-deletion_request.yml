post:
  summary: Sends an email to delete all data linked to a user
  description: Sends an email to delete all data linked to a user
  tags:
    - Users
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          properties:
            email:
              type: string
  responses:
    "400":
      $ref: "../../responses.yml#/400"
    "204":
      $ref: "../../responses.yml#/204"
    "500":
      $ref: "../../responses.yml#/500"
