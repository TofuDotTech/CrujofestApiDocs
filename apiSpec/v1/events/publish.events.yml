leaveParty:
  description: Leave the room associated to the party and disconnects the socket connection
  name: leaveParty
  payload:
    type: string
    description: Party code
play:
  description: Resume the currently playing song
  name: play
pause:
  description: Pause the currently playing song
  name: pause
next:
  description: Play the next song in the queue
  name: next
previous:
  description: Play the previous song in the queue
  name: previous
querySong:
  description: Add a song to the queue
  name: querySong
  payload:
    type: string
    description: query
songChanged:
  description: Notify the server that the currently playing song has changed
  name: songChanged
  payload:
    type: string
    description: Spotify song URI
paused:
  description: Notify the server that the currently playing song was paused or resumed
  name: paused
  payload:
    type: boolean
    description: flag
currentSong:
  description: Get current song playing
  name: currentSong
  x-ack:
    args:
      type: object
      properties:
        status:
          type: number
        currentSong:
          nullable: true
          $ref: ../schemas.yml#/currentSongSchema
getQueue:
  description: Get queue
  name: getQueue
  x-ack:
    args:
      type: object
      properties:
        status:
          type: number
        partyQueue:
          type: array
          Description: JSON strings with the following schemas
          items:
            $ref: ../schemas.yml#/resultItemSchema
        queuePointer:
          type: number
